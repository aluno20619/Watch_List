@model Watch_List.Models.Profissao

@{
    ViewData["Title"] = "Profissão";
}

<h1>Detalhes</h1>

<div>
    <h4>@ViewBag.Title</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Tarefa)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Tarefa)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Gestor") || User.IsInRole("Funcionario"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Editar</a> 
    }
<a asp-action="Index">Voltar à página anterior</a>
</div>
